// ----------------------------------------------------------------------------
// DEPRECATED — superseded by HUDStack.tsx (right column) + EncryptionProgress
// / NodeWidget (left column), part of the AI Intelligence Core creative
// pivot. Left as an inert stub because this workspace doesn't allow file
// deletion.
// ----------------------------------------------------------------------------

export {};
