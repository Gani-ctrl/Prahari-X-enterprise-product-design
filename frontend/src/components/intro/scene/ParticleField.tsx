// ----------------------------------------------------------------------------
// DEPRECATED — no longer used. See CameraRig.tsx in this same directory for
// why: the intro no longer uses React Three Fiber / Three.js at all — its
// replacement (SmokeLayer.tsx, one level up) is a DOM/CSS particle effect
// instead. Left as an inert stub because this workspace doesn't allow file
// deletion.
// ----------------------------------------------------------------------------

export {};
