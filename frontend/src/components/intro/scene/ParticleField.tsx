// ----------------------------------------------------------------------------
// DEPRECATED — archived per explicit creative-pivot direction: particles are
// now a Canvas2D dust/ember field (components/intro/AtmosphereCanvas.tsx)
// rather than a React Three Fiber GPU point-sprite system, since the intro
// no longer uses R3F/Three.js at all. Left as an inert stub because this
// workspace doesn't allow file deletion.
// ----------------------------------------------------------------------------

export {};
