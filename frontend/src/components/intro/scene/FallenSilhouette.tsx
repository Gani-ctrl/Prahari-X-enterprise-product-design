// ----------------------------------------------------------------------------
// DEPRECATED — no longer used. See CameraRig.tsx in this same directory for
// why: the intro no longer uses React Three Fiber / Three.js at all. Left as
// an inert stub because this workspace doesn't allow file deletion.
// ----------------------------------------------------------------------------

export {};
