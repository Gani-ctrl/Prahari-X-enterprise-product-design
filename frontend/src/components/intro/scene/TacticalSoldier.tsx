// ----------------------------------------------------------------------------
// DEPRECATED — archived per explicit creative-pivot direction: the intro no
// longer features a soldier, human character, weapon, or any character
// silhouette in any form. The hero element is now the AI Intelligence Core
// (see components/intro/core/AICore.tsx). Left as an inert stub because
// this workspace doesn't allow file deletion. Do not resurrect or extend
// this component.
// ----------------------------------------------------------------------------

export {};
