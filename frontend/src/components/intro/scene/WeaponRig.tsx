// ----------------------------------------------------------------------------
// DEPRECATED — no longer used. See CameraRig.tsx in this same directory for
// why: the intro no longer uses React Three Fiber / Three.js at all, and
// there is no first-person weapon in the rebuilt DOM/CSS + GSAP intro. Left
// as an inert stub because this workspace doesn't allow file deletion.
// ----------------------------------------------------------------------------

export {};
