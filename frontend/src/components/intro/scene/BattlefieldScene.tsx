// ----------------------------------------------------------------------------
// DEPRECATED — archived per explicit creative-pivot direction: no soldiers,
// battlefields, wreckage, or military vehicles in the intro anymore, and no
// React Three Fiber canvas at all. The new hero is the AI Intelligence Core
// — see components/intro/core/AICore.tsx — set against a DOM/CSS +
// Canvas2D atmosphere (components/intro/AtmosphereCanvas.tsx). Left as an
// inert stub because this workspace doesn't allow file deletion.
// ----------------------------------------------------------------------------

export {};
