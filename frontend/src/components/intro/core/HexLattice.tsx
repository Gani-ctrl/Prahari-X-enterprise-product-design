// ----------------------------------------------------------------------------
// DEPRECATED — superseded by TacticalGlobe.tsx's own hex-cell bands +
// geodesic ("triangular topology") shell, now built directly onto the
// globe's surface instead of as a separate free-floating lattice. Left as
// an inert stub because this workspace doesn't allow file deletion.
// ----------------------------------------------------------------------------

export {};
