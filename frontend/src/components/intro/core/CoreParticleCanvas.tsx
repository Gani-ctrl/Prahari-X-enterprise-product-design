// ----------------------------------------------------------------------------
// DEPRECATED — removed as part of eliminating every DOM layer around the AI
// Core (see CoreGlassOverlay.tsx for why). "Tiny particles orbiting the
// globe" now live entirely inside the R3F scene (drei Sparkles in
// OrbitRings.tsx), which actually orbit the globe in 3D rather than
// drifting on a flat 2D canvas layer in front of it. Left as an inert stub
// because this workspace doesn't allow file deletion.
// ----------------------------------------------------------------------------

export {};
