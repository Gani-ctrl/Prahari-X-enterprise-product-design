// ----------------------------------------------------------------------------
// DEPRECATED — superseded by TacticalGlobe.tsx, which now owns its own dark
// glass shell + custom-GLSL Fresnel rim directly (per the "Tactical AI
// Reactor" redesign: the glass shell is an attribute of the globe itself,
// not a separate sphere wrapping the whole ring system). Left as an inert
// stub because this workspace doesn't allow file deletion.
// ----------------------------------------------------------------------------

export {};
