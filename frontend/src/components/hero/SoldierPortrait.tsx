// ----------------------------------------------------------------------------
// DEPRECATED — archived per explicit creative-pivot direction: no soldiers,
// human characters, weapons, or character silhouettes anywhere in the app's
// intro/hero experience anymore. The intro's hero element is now the AI
// Intelligence Core — see components/intro/core/AICore.tsx. This entire
// components/hero/ folder (built for a previous "static hero screen" pass)
// is retired; left as an inert stub because this workspace doesn't allow
// file deletion. Do not resurrect or extend.
// ----------------------------------------------------------------------------

export {};
